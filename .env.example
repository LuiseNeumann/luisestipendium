# Optional: Ohne Schlüssel verwendet der Sparberater lokale, datenbasierte Antworten.
OPENAI_API_KEY=
OPENAI_CHAT_MODEL=gpt-4o-mini

# Optional: Standard ist data/streaming.db.
DATABASE_PATH=data/streaming.db
